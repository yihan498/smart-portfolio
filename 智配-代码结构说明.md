# 智配 · 代码结构说明

两个文件必须放在**同一目录**下才能互相跳转。

```
智配-产品介绍.html  （55KB，818行）← 产品介绍页
智配-计算器.html    （37KB，492行）← 交互计算器页
```

---

## 两个文件的关系

```
智配-产品介绍.html
  ├── 导航「立即使用」→ href="智配-计算器.html"
  ├── Hero CTA按钮    → href="智配-计算器.html"
  └── 使用方法步骤1   → href="智配-计算器.html"

智配-计算器.html
  └── Header「返回产品介绍」→ href="智配-产品介绍.html"
```

---

---

# 文件一：`智配-产品介绍.html`（818行）

## 整体结构

```
<head>           第1-287行    ← 样式定义
<body>           第288-817行  ← 页面内容 + JS
```

---

## CSS 样式段落（第9-286行）

```css
/* 第10-14行：CSS变量 */
:root { --obsidian, --gold, --btc, --us, --cn, --def ... }
/* 定义全局颜色系统，深黑底色 + 金色主调 + 四类资产专属色 */

/* 第25-34行：导航栏样式 */
nav { position:fixed ... }
/* 顶部固定导航，含品牌logo + 页面内锚点链接 + 「立即使用」金色按钮 */

/* 第35-71行：Hero 区域样式 */
#hero { min-height:100vh ... }
/* 全屏首屏，左侧文案 + 右侧RS演示卡片的两栏布局 */

/* 第72-77行：通用Section标题样式 */
.sec-header, .sec-tag, .sec-title, .sec-desc
/* 所有章节共用的标题、副标题、装饰标签样式 */

/* 第79-91行：问题区 */
#problem, .problem-grid, .problem-card
/* 三列问题卡片，顶部有颜色线条区分三种问题 */

/* 第93-120行：四类资产区 */
#assets, .asset-card, .asset-icon, .asset-badge
/* 2x2网格卡片，BTC/美股/中国/防守各有专属边框色 */

/* 第121-132行：三步流程区 */
#process, .process-steps, .step-num, .step-formula
/* 横排三步骤，圆形编号 + 金色连接线 + 公式代码框 */

/* 第133-154行：双引擎架构区 */
#engine, .engine-card, .engine-formula, .engine-connector
/* 两列引擎卡片（金色/蓝色），下方连接公式条 + 洞见段落 */

/* 第156-168行：11因子区 */
#factors, .factor-row, .factor-weight-wrap, .factor-bar-track
/* 2列因子列表，每行含代码/名称/权重条形 */

/* 第170-191行：配置示例区 */
#allocations, .alloc-card, .stack-bar, .stack-seg
/* 2x2示例卡片，彩色堆叠条形图 + 图例列表 */

/* 第192-241行：使用方法区 */
#howto, .how-step, .how-num
/* 步骤列表，圆形编号 + 竖线连接 */

/* 第253-263行：免责区 + 底部 */
#disclaimer, .disc-item, footer

/* 第265-286行：响应式 + 滚动动画 */
@media(max-width:768px) { ... }
.reveal, .reveal.visible  /* 滚动入场动画 */
```

---

## HTML 页面内容（第288-790行）

| 行数 | 对应页面区域 | 内容 |
|---|---|---|
| 290–302 | **顶部导航栏** | logo + 问题/原理/示例锚点 + 「立即使用」→ 跳转计算器 |
| 304–364 | **Hero 首屏** | 大标题、slogan、两个CTA按钮；右侧RS=6.5演示卡片（纯静态展示） |
| 368–397 | **为什么需要它** | 三个问题卡片：凭直觉/复制别人/比例无依据 |
| 401–460 | **四类资产** | BTC/美股/中国资产/防守资产卡片，含μ/σ/Sharpe参数 |
| 464–496 | **三步工作原理** | 步骤1→RS公式 / 步骤2→σ公式 / 步骤3→MVO数学表达式 |
| 500–531 | **双引擎架构** | 引擎一（11因子打分）+ 引擎二（MVO优化）+ 整合逻辑说明 |
| 535–601 | **11个评估因子** | F1-F11逐条展示，含权重比例条 |
| 605–696 | **配置示例** | RS=2/5/7/9 四个场景的堆叠条形图 |
| 700–752 | **使用方法** | 五步操作说明（点击立即使用→填写→选择→查看→定期调整） |
| 757–783 | **模型局限性** | 参数不确定性/正态分布假设/相关性时变/建议使用方式 |
| 785–789 | **页脚** | 品牌名 + 技术栈说明 + 免责声明 |

---

## JavaScript（第791-816行）

```javascript
// 第793-796行：滚动入场动画
IntersectionObserver → 元素进入视口时添加 .visible 类 → CSS触发淡入上移

// 第798-809行：Hero区RS数字滚动动效
0 → 6.5 的计数动画，1.8秒easeOut，页面加载0.4秒后启动

// 第811-815行：滚动时导航栏变实色
window.scroll → scrollY>80时导航背景从渐变变为纯色不透明
```

---

---

# 文件二：`智配-计算器.html`（492行）

## 整体结构

```
<head>          第1-159行     ← 样式定义
<body>          第160-491行   ← 页面内容 + JS
```

---

## CSS 样式段落（第9-158行）

```css
/* 第10-15行：CSS变量（与产品介绍完全一致的颜色系统） */

/* 第20-35行：顶部 Header 样式 */
header { position:sticky ... }
/* 计算器页面头部：固定吸顶，含品牌logo + 「返回产品介绍」链接 */

/* 第37-38行：主体双栏布局 */
main { display:grid; grid-template-columns:360px 1fr }
/* 左360px输入面板 + 右侧弹性结果面板 */

/* 第40-88行：左面板（输入区）样式 */
.panel-left, .panel-section-title, .field, .field label
.field input[type=number], .field select
.slider-row, .mini-bar-wrap, .mini-bar-fill
/* 数字输入框/下拉选择框/滑块/字段标签/每个字段下方的2px迷你得分条 */

/* 第89-109行：右面板 RS得分卡样式 */
.rs-card, .rs-big-num, .rs-gauge-track, .rs-desc
/* 金色边框大卡，左侧数字块 + 右侧进度条+描述文字 */

/* 第111-124行：配置比例条样式 */
.alloc-card, .stack-bar, .stack-seg, .legend-grid
/* 彩色堆叠条形图 + 2列图例 */

/* 第126-130行：三格统计数据样式 */
.stats-row, .stat-cell, .stat-val, .stat-lbl
/* 收益率/波动率/夏普比 三格横排 */

/* 第132-141行：11因子得分明细样式 */
.fscore-card, .fscore-grid, .fscore-row
.fscore-bar, .fscore-bar-fill, .fscore-val
/* 2列网格，每行：因子代码 + 名称 + 小进度条 + 数字 */

/* 第143-145行：文字解读卡 */
.interp-card { font-size:.85rem; line-height:1.8 }

/* 第151-158行：移动端响应式 */
@media(max-width:800px) { main → 单列; 面板高度限制 }
```

---

## HTML 页面内容（第161-362行）

| 行数 | 对应页面区域 | 内容 |
|---|---|---|
| 163–175 | **顶部固定Header** | 「智配 · 配置计算器」logo + v2.1标签 + 「← 返回产品介绍」链接 |
| 177–293 | **左面板：输入区** | |
| 181–219 | ↳ 财务指标（F1-F5，自动计算） | 年龄/年收入/年支出/总资产/紧急储备金/负债，每项下方有迷你得分条 |
| 222–292 | ↳ 主观评估（F6-F11） | F6-F10五个下拉菜单 + F11滑块（1-10） |
| 295–357 | **右面板：结果区** | |
| 298–310 | ↳ RS得分卡 | 大字显示RS分数 + 类型描述 + σ公式值 + 进度条 + 文字解读 |
| 312–327 | ↳ 配置比例条 | 四色堆叠条形图 + BTC/美股/中国/防守四行图例 |
| 329–334 | ↳ 三格统计 | 预期年化收益率 / 组合年化波动率 / 夏普比率 |
| 336–352 | ↳ 11因子得分明细 | F1-F11逐行：代码 + 名称+权重 + 进度条 + 分值（预建于HTML） |
| 354–355 | ↳ 文字解读卡 | 基于RS区间的个性化解读段落 |
| 360–362 | **底部免责栏** | 小字声明 |

---

## JavaScript（第364-490行）

```javascript
// 第365行：MVO查找表（内联JSON，101个档位）
const MVO = { "0.0": {vol, btc, us, cn, def, ret, pvol, sharpe}, ... "10.0": {...} }
// RS 0.0~10.0，步长0.1，共101条记录，每条含四类资产权重+组合统计

// 第367行：查找函数
function lookup(rs) { return MVO[(Math.round(rs*10)/10).toFixed(1)]; }
// RS值四舍五入到0.1精度，映射到MVO表对应行

// 第369-381行：因子元数据定义
const FACTORS = [ {id:'f1', code:'F1', name:'年龄', w:.20}, ... ]
// 11个因子的代码/名称/权重，用于遍历更新显示

// 第383-390行：类型判断函数
function rsLevel(rs) → 返回「保守型/稳健型/平衡型/积极型/进攻型/极限进攻型」

// 第392-398行：文字解读生成函数
function interp(rs, d) → 根据RS区间 + MVO数据，生成含具体百分比的个性化描述段落

// 第400-473行：核心计算函数 calc()
// ① 读取所有输入值（6个数字框 + 5个下拉 + 1个滑块）
// ② 计算F1-F5得分（数学公式）
// ③ F6-F11直接读取选项/滑块值
// ④ 加权求和得到RS
// ⑤ 更新11因子明细行（进度条宽度 + 分值文字）
// ⑥ 更新左面板迷你得分条 + 字段标签
// ⑦ 更新RS卡（大数字/类型/σ值/进度条/描述）
// ⑧ 更新配置比例条（宽度/百分比标注/图例数字）
// ⑨ 更新三格统计数据
// ⑩ 更新底部文字解读卡

// 第476-485行：事件监听器
// 6个数字输入框 → oninput → calc()
// 5个下拉选择  → onchange → calc()
// 滑块 F11    → oninput → 更新显示数字 + calc()

// 第488行：页面加载时立即执行一次
calc();  // 用默认值初始化所有结果显示
```
